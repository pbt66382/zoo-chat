# Zoo AI Chat - Phase 2 依赖包

# AI/ML 核心
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# LLM 提供方 - DeepSeek（通过 langchain-community 的 ChatOpenAI 调 OpenAI 兼容接口）
# ChatOpenAI 内部仍依赖 openai SDK，必须显式安装
openai>=1.0.0

# Milvus 向量数据库
pymilvus>=2.3.0
langchain-milvus>=0.1.0

# 本地 Embedding（faq_chain 使用 HuggingFaceEmbeddings）
langchain-huggingface>=0.1.0
sentence-transformers>=2.2.0

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP 客户端
httpx>=0.27.0

# 环境变量管理
python-dotenv>=1.0.1

# YAML 配置/日志
pyyaml>=6.0

# 测试
pytest>=8.2.0
pytest-asyncio>=0.23.0
